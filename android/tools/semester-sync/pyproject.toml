[project]
name = "semester-sync"
version = "0.1.0"
description = "学期时间同步管理工具 - 管理学期开学时间并上传到阿里云 OSS"
requires-python = ">=3.11"
dependencies = [
    "opendal>=0.45.0",
    "textual>=1.0.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
