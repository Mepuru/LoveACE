# LoveACE

Monorepo snapshot for the native LoveACE clients.

- `android/` — Android client
- `ios/` — iOS client

Historical branches:

- `flutter-ver` — previous Flutter implementation
- `backend-old` — previous backend repository snapshot
